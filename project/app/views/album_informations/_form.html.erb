<%= form_with(model: album_information, local: true) do |form| %>
  <% if album_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album_information.errors.count, "error") %> prohibited this album_information from being saved:</h2>

      <ul>
      <% album_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :play_count %>
    <%= form.number_field :play_count %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
